machine:

  timezone:
    Europe/London

  node:
    version: 6.0.0

  environment:
    CONTINUOUS_INTEGRATION: true
#    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

#dependencies:
#  override:
#    - yarn
#  cache_directories:
#    - ~/.cache/yarn
#
#test:
#  override:
#    - yarn test

general:
  build_dir: web
